# FastAPI. Bumped from 0.109.0: it caps starlette<0.36, but mcp==1.0.0
# needs starlette>=0.39.
fastapi==0.115.6
uvicorn[standard]==0.27.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (unmaintained since 2020) is incompatible with bcrypt>=4:
# its backend self-test raises ValueError on bcrypt's newer 72-byte
# handling, breaking every password hash/verify call. Pin to the last
# compatible 3.x line.
bcrypt==3.2.2

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
redis==5.0.1

# LangChain & LangGraph. Bumped off the 0.1.5-era pins (Jan 2024): that
# generation's langchain-core hard-caps packaging<24.0, which is
# unconditionally incompatible with e2b (packaging>=24.1 on every e2b
# release, checked back to 1.0.0). Ranged rather than exact-pinned so pip
# can pick a mutually consistent set within the 0.2.x generation.
langchain>=0.2.0,<0.3.0
langchain-openai>=0.1.0,<0.2.0
langchain-anthropic>=0.1.15,<0.2.0
langchain-google-genai>=1.0.0,<2.0.0
langgraph>=0.1.0,<0.2.0
langchain-community>=0.2.0,<0.3.0

# Data validation. pydantic bumped from 2.5.3: mcp==1.0.0 requires >=2.8.0.
# email-validator is required at import time by schemas/user.py's EmailStr
# fields -- pydantic's EmailStr defers to it and raises ImportError without it.
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0

# HTTP & WebSocket
websockets==12.0
httpx==0.27.2
aiohttp==3.9.1

# Utilities
python-dotenv==1.0.0
PyYAML==6.0.1
# Used by services/compiler/langgraph_compiler.py to render generated
# workflow code -- not previously in requirements.txt at all.
jinja2==3.1.4
# Used by api/endpoints/datasets.py -- also not previously listed.
pandas==2.2.3

# E2B Code Interpreter for secure code execution.
# Pinned (rather than the previous open-ended >=) because pip's resolver
# was hitting "resolution-too-deep" trying to search the combined version
# space of these against the old langchain==0.1.5-era pins above.
e2b-code-interpreter==1.0.5
mcp==1.0.0
anthropic==0.40.0
